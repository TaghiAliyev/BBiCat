# This is a test script.

# Idea is to see, if I send something to R first and then call a script
# Will the script recognize that variable?
# Result: it does!
print(a)

for (i in 1:length(clusters))
{
print(clusters[i]);
}